# RUN10 — CAPABILITY LIFECYCLE MATRIX (CAP-01E-v2)
# 2026-08-22 · kernel 6.18.12+kali-amd64 · BPF LSM
# Kernel: tx_cap{dev,ino,tgid,owner_uid,expires_ns}; ATTACH checks
#         exists ∧ single-use ∧ tgid ∧ uid ∧ now<expires (bpf_ktime_get_ns)
# Issuer: /run/protectme/tx.sock SEQPACKET; SO_PEERCRED; TTL clamp ≤60s;
#         root_owner map written at PM_INODE_SET registration time.

== R10-01 CROSS-TASK, FRESH VALID nonce (bound to shell pid, presented by other pm-tx process)
$ pm-tx request /tmp/pm-L1        -> 0xaf8d7573236105e1
$ pm-tx attach 0xaf8d7573236105e1 (different pid)
$ unlink /tmp/pm-L1/file          -> Operation not permitted          [DENY ✅]

== R10-02 EXPIRY (ttl=1500ms; attach after sleep 2s from same pid via bash -c)
attach presented nonce=0x6109a30afc3ed759
unlink                            -> Operation not permitted          [DENY ✅]

== R10-03 *** INVALID TEST *** ($() command substitution spawned a subshell,
   so request ran as PID A and attach as PID B — the test did not measure
   what it intended)
   Kernel behavior observed: unlink -> Operation not permitted.
   Reclassified as ADDITIONAL cross-task evidence (nonce possession alone
   is insufficient; capability binds to execution identity), superseded by
   the deliberate R10-01 fresh-nonce test. Positive control = R10-04.

== R10-04 EXEC KEEPS TX (positive control for request+attach+destroy same chain)
$ pm-tx run-auth /tmp/pm-L1 -- rm -f /tmp/pm-L1/file -> rc=0           [ALLOW ✅]

== R10-05 FORK LOSES TX (sh -c '( unlink ... )' subshell child)
unlink                            -> Operation not permitted
file survives: YES                                                     [DENY ✅]

== R10-06 HOLDER SIGKILL MID-HOLD (pm-tx run-auth -- sleep 30; kill -9)
sibling unlink                    -> Operation not permitted
L2 file survives: YES                                                  [NO-INHERIT ✅]

== R10-07 CROSS-UID CLAIM (issuer policy, root_owner map)
   NOTE: first attempt VOID — executed after R10-08 restart wiped maps;
   redone on freshly registered state (methodology lesson).
$ sudo -u nobody pm-tx request /tmp/pm-L1  -> 0x0                     [REJECT ✅]
[capiss] DENIED cross-uid: root owned by 1000, peer 65534
control: owner uid1000 request -> issued 0xf78b459e12f80084            [ALLOW ✅]

== R10-08 LOADER RESTART (pkill -9 + relaunch)
post-restart unlink               -> ALLOWED (maps empty)
=> ALL capability + protection state is VOLATILE by design.
   Research limitation; production requires pinned maps + explicit
   re-registration flow. Reboot semantics identical.                   [✅ documented]

## SUMMARY
lifecycle denials: cross-task(fresh), expiry, fork, SIGKILL-inheritance,
cross-uid claim, replay-after-consume (RUN9 T5) — ALL REJECTED
positives: exec-chain destroy, same-root destroy, owner issuance — ALL ALLOWED
unproven/next: FD-based kernel object (close(fd)=revoke), pinned-map policy
store in protectmed, multi-user hardening beyond owner-uid rule.
