# RUN15 — REVOKE / RENAME-OUT / DEAD-STATE (P0.5) — 2026-08-22

## REVOKE (epoch-based)
- cap_epoch array map, global counter.
- REGF stamps cap.epoch = epoch_get().
- ATTFD rejects if cap.epoch != epoch_get().
- RVKE prctl (root-only) bumps epoch → every outstanding FD cap dies instantly,
  O(1), no map iteration.
- Issuer protocol PMB5: single uint32 magic; server calls RVKE, replies nonce=0xE0CA.
- pm-tx revoke subcommand works; stale fd presentations after revoke denied.
- Verified: request cap → revoke → run-auth with new request works; stale fd dead.

## PATH_RENAME hook (closes R14-04 exfiltration gap)
- vfs_rename calls security_path_rename (NOT inode_rename) for local renames.
- Added SEC("lsm/path_rename") with nearest_root on source and destination parent.
- Classification:
    source outside any root                 → allow
    source inside, dest same root            → cls 1 (boundary op)
    source inside, dest outside/different    → cls 2 (destructive exfiltration)
- compute_verdict reused → mode 2 denies all boundary renames without TX;
  mode 1 allows within-tree, denies cross-boundary (cls 2).
- Tested: mode2 rename-out EPERM ✓; mode1 within-tree allowed ✓;
  mode1 rename-out EPERM ✓.

## DEAD-state heartbeat
- Loader writes /run/protectme/state every ~1s: STATE=ACTIVE time=...
- Clean shutdown writes STATE=STOPPED.
- SIGKILL leaves stale file; pm-tx status reads age <5s ⇒ ACTIVE,
  else DEAD. Verified status subcommand.

## LOADER BUG FIX (revisit)
- SIGHUP poll EINTR errno clobber fixed earlier; 5×HUP stable.

All P0.5 objectives met.